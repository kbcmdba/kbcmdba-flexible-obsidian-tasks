{
	"name": "flexible-tasks",
	"version": "0.9.1",
	"description": "Interactive task checkboxes in Markdown table cells and lists, with custom statuses styled by your theme.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"obsidian-plugin",
		"tasks",
		"table",
		"checkbox"
	],
	"author": "KB Benton",
	"license": "GPL-2.0-only",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"esbuild": "^0.20.2",
		"obsidian": "latest",
		"typescript": "^5.4.5"
	}
}
